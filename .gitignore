# this is a file that mac computers produce, not needed for others on the team
.DS_Store

# make api keys private
scripts/firebase-api.js

# hosting files
.firebase
.firebase/hosting..cache
.firebaserc
404.html
firebase.json
firestore.indexes.json
firestore.rules
